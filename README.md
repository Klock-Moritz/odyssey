# Odyssey Hypermedia Explorer

The Odyssey Hypermedia Explorer is a sophisticated client for RESTful web
services that use hypermedia as the engine of application state (HATEOAS).
It currently supports web linking using the "Link" HTTP header and the
Hypertext Application Language (HAL).
